{
  "//": "ESM output. `module: esnext` emits import/export verbatim, so the explicit .js specifiers the source carries resolve under Node ESM. We cannot use node16 here: with no root `type` field tsc classifies every source file as CommonJS. dist/esm is marked type:module by scripts/postbuild.mjs.",
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "module": "esnext",
    "moduleResolution": "bundler",
    "outDir": "./dist/esm",
    "declaration": true,
    "declarationMap": true,
    "tsBuildInfoFile": "./dist/.tsbuildinfo-esm"
  }
}
